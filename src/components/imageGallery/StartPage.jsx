import React from 'react';

const StartPage = () => {
    const styled = {
        textAlign: 'center',
        color: '#ccc',
    };
    return (
        <div style={styled}>
            <h1>Enter your first request</h1>
        </div>
    );
};

export default StartPage;
